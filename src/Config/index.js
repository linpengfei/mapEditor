/**
 * @author:lpf
 * @flow
 *
 **/
const meun = [{
  key: 'file',
  label: '文件',
  children: [{
    
  }]
}];

export const extrudeSettings = {
  steps: 1,
  depth: 100,
  curveSegments: 100,
  bevelEnabled: false,
};